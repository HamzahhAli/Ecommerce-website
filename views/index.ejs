<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <style>
    body, html {
      margin: 0;
      padding: 0;
    }
  </style>
</head>
<body>
  <%- include('components/Navbar') %>

  <div id="bannerCarousel" class="carousel slide" data-ride="carousel" data-interval="3000" style="margin-top: 20px;">
    <div class="carousel-inner">
      <% banners.forEach((banner, index) => { %>
        <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
          <img src="<%= banner.imageUrl %>" alt="<%= banner.title %>" class="img-fluid" style="height: 300px; object-fit: cover; width: 100%;">
          <div class="carousel-caption d-none d-md-block">
            <h2><%= banner.title %></h2>
            <% if (banner.link) { %>
              <a href="<%= banner.link %>" class="btn btn-primary">Learn More</a>
            <% } %>
          </div>
        </div>
      <% }); %>
    </div>
    <a class="carousel-control-prev" href="#bannerCarousel" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#bannerCarousel" role="button" data-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>

  <% if (sections.featured.length > 0) { %>
  <div class="container mt-4">
    <h3>Featured Products</h3>
    <div class="featured-products-container" id="featured">
      <button class="scroll-button left" onclick="scrollSection('featured', -250)">&#10094;</button>
      <div class="featured-products">
        <% sections.featured.forEach(product => { %>
        <a href="/products/<%= product._id %>" class="card product-card">
          <img src="<%= product.imageUrl %>" class="card-img-top" alt="<%= product.name %>">
          <div class="card-body">
            <h3>$<%= product.price %></h3>
            <h3 class="card-title"><%= product.name %></h3>
          </div>
        </a>
        <% }); %>
      </div>
      <button class="scroll-button right" onclick="scrollSection('featured', 250)">&#10095;</button>
    </div>
  </div>
  <% } %>

  <% if (sections.greatDeals.length > 0) { %>
  <div class="container mt-4">
    <h3>Great Deals</h3>
    <div class="featured-products-container" id="greatDeals">
      <button class="scroll-button left" onclick="scrollSection('greatDeals', -250)">&#10094;</button>
      <div class="featured-products">
        <% sections.greatDeals.forEach(product => { %>
        <a href="/products/<%= product._id %>" class="card product-card">
          <img src="<%= product.imageUrl %>" class="card-img-top" alt="<%= product.name %>">
          <div class="card-body">
            <h3>$<%= product.price %></h3>
            <h3 class="card-title"><%= product.name %></h3>
          </div>
        </a>
        <% }); %>
      </div>
      <button class="scroll-button right" onclick="scrollSection('greatDeals', 250)">&#10095;</button>
    </div>
  </div>
  <% } %>

  <% if (sections.newArrivals.length > 0) { %>
  <div class="container mt-4">
    <h3>New Arrivals</h3>
    <div class="featured-products-container" id="newArrivals">
      <button class="scroll-button left" onclick="scrollSection('newArrivals', -250)">&#10094;</button>
      <div class="featured-products">
        <% sections.newArrivals.forEach(product => { %>
        <a href="/products/<%= product._id %>" class="card product-card">
          <img src="<%= product.imageUrl %>" class="card-img-top" alt="<%= product.name %>">
          <div class="card-body">
            <h3>$<%= product.price %></h3>
            <h3 class="card-title"><%= product.name %></h3>
          </div>
        </a>
        <% }); %>
      </div>
      <button class="scroll-button right" onclick="scrollSection('newArrivals', 250)">&#10095;</button>
    </div>
  </div>
  <% } %>

  <% if (sections.topRated.length > 0) { %>
  <div class="container mt-4">
    <h3>Top Rated</h3>
    <div class="featured-products-container" id="topRated">
      <button class="scroll-button left" onclick="scrollSection('topRated', -250)">&#10094;</button>
      <div class="featured-products">
        <% sections.topRated.forEach(product => { %>
        <a href="/products/<%= product._id %>" class="card product-card">
          <img src="<%= product.imageUrl %>" class="card-img-top" alt="<%= product.name %>">
          <div class="card-body">
            <h3>$<%= product.price %></h3>
            <h3 class="card-title"><%= product.name %></h3>
          </div>
        </a>
        <% }); %>
      </div>
      <button class="scroll-button right" onclick="scrollSection('topRated', 250)">&#10095;</button>
    </div>
  </div>
  <% } %>

  <%- include('components/Footer') %>
  <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
  <script src="/scripts.js"></script>

</body>
</html>
